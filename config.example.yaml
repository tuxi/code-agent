model:
  provider: deepseek
  base_url: "https://api.deepseek.com"
  # model must support function calling
  model: "deepseek-v4-flash"
  temperature: 0.2

agent:
  max_steps: 16

workspace:
  root: "."